To run the program:

g++ -o p0 p0.cpp -O3 -lm -fopenmp
./p0

Using command "uptime" to check the system load to see how much off your wall-clock time measurement will be due to the competiyion from other users.
Three numbers represent total CPU load averages for last 1, 5, 15 minutes respectively.
